import { ISearch } from './Search';

const base: ISearch = {
  sampleTextProp: 'Hello World!',
};

const alt: ISearch = {
  sampleTextProp: 'Good Morning!',
};

export const mockSearchProps = { base, alt };
