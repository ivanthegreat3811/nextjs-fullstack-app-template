import { IBaseTemplate } from './BaseTemplate';

const base: IBaseTemplate = {
  sampleTextProp: 'Hello World!',
};

const alt: IBaseTemplate = {
  sampleTextProp: 'Good Morning!',
};

export const mockBaseTemplateProps = { base, alt };
